'use client';\n\nimport React from 'react';\nimport { Zap, Brain, Moon, Sparkles } from 'lucide-react';\n\nexport type StateFilterType = 'all' | 'energy' | 'meditation' | 'relax';\n\ninterface StateFilterProps {\n  activeState: StateFilterType;\n  onSelectState: (state: StateFilterType) => void;\n}\n\nexport const StateFilter: React.FC<StateFilterProps> = ({\n  activeState,\n  onSelectState,\n}) => {\n  const filters: { id: StateFilterType; label: string; icon: React.ReactNode; desc: string }[] = [\n    {\n      id: 'all',\n      label: 'Все сборы',\n      icon: <Sparkles className=\"w-4 h-4\" />,\n      desc: 'Полная селекционная коллекция свежего урожая',\n    },\n    {\n      id: 'energy',\n      label: 'Бодрость и фокус',\n      icon: <Zap className=\"w-4 h-4 text-amber-500\" />,\n      desc: 'Высокий теанин и полифенолы для ясного ума без тремора',\n    },\n    {\n      id: 'meditation',\n      label: 'Спокойствие и медитация',\n      icon: <Brain className=\"w-4 h-4 text-emerald-600\" />,\n      desc: 'ГАМК-аминокислоты для снятия тревоги и погружения в поток',\n    },\n    {\n      id: 'relax',\n      label: 'Вечерний релакс',\n      icon: <Moon className=\"w-4 h-4 text-indigo-400\" />,\n      desc: 'Минимум кофеина, мягкие цветочные настои для глубокого сна',\n    },\n  ];\n\n  return (\n    <div className=\"w-full bg-[#FAF7F2] py-8 border-b border-[#E2D7CC]\">\n      <div className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8\">\n        <div className=\"text-center max-w-2xl mx-auto mb-6\">\n          <span className=\"text-xs font-semibold uppercase tracking-[0.2em] text-[#89AA3E] font-sans\">\n            Подбор по состоянию\n          </span>\n          <h2 className=\"font-display text-3xl sm:text-4xl text-[#035224] tracking-wide mt-1 uppercase\">\n            Чай как инструмент для вашего дня\n          </h2>\n        </div>\n\n        {/* Filter Pills */}\n        <div className=\"flex flex-wrap items-center justify-center gap-3 sm:gap-4\">\n          {filters.map((f) => {\n            const isActive = activeState === f.id;\n            return (\n              <button\n                key={f.id}\n                onClick={() => onSelectState(f.id)}\n                className={`flex items-center gap-2.5 px-5 py-3 rounded-full text-xs sm:text-sm font-semibold tracking-wide transition-all duration-200 shadow-sm ${\n                  isActive\n                    ? 'bg-[#035224] text-white shadow-md scale-105 ring-2 ring-[#035224]/20'\n                    : 'bg-[#EEE1D5]/70 text-[#121A14] hover:bg-[#EEE1D5] hover:text-[#035224]'\n                }`}\n              >\n                {f.icon}\n                <span>{f.label}</span>\n              </button>\n            );\n          })}\n        </div>\n\n        {/* Dynamic State Description */}\n        <p className=\"text-center text-xs sm:text-sm text-[#667069] mt-4 font-serif italic max-w-xl mx-auto\">\n          {filters.find((f) => f.id === activeState)?.desc}\n        </p>\n      </div>\n    </div>\n  );\n};\n