'use client';

import React, { useState } from 'react';
import { TeaProduct, TeaWeightOption } from '@/data/teaProducts';
import { ShoppingBag, Check, Thermometer, Clock, Sparkles } from 'lucide-react';

interface ProductCardProps {
  product: TeaProduct;
  onAddToCart: (product: TeaProduct, weightOption: TeaWeightOption) => void;
  onOpenBrewingModal?: (product: TeaProduct) => void;
}

export const ProductCard: React.FC<ProductCardProps> = ({\n  product,\n  onAddToCart,\n  onOpenBrewingModal,\n}) => {\n  const [selectedWeight, setSelectedWeight] = useState<TeaWeightOption>(\n    product.weights.find((w) => w.isPopular) || product.weights[0]\n  );\n  const [isAdded, setIsAdded] = useState(false);\n\n  const handleAdd = () => {\n    onAddToCart(product, selectedWeight);\n    setIsAdded(true);\n    setTimeout(() => setIsAdded(false), 1800);\n  };\n\n  const pricePerGram = Math.round(selectedWeight.price / selectedWeight.weight);\n\n  return (\n    <div className=\"bg-white rounded-2xl border border-[#E2D7CC] overflow-hidden shadow-soft hover:shadow-card transition-all duration-300 flex flex-col group hover:-translate-y-1\">\n      {/* Product Image Container */}\n      <div className=\"relative aspect-[4/3] bg-[#F3ECE3] overflow-hidden\">\n        <img\n          src={product.image}\n          alt={product.nameRu}\n          className=\"w-full h-full object-cover group-hover:scale-105 transition-transform duration-700 ease-out\"\n          loading=\"lazy\"\n        />\n\n        {/* Top Badges */}\n        <div className=\"absolute top-3 left-3 flex flex-col gap-1.5 items-start\">\n          {product.tag && (\n            <span className=\"px-2.5 py-1 rounded-full bg-[#D4583D] text-white text-[11px] font-bold uppercase tracking-wider shadow-sm\">\n              {product.tag}\n            </span>\n          )}\n          <span className=\"px-2.5 py-0.5 rounded-full bg-[#035224]/85 backdrop-blur-md text-[#EEE1D5] text-[10px] font-medium tracking-wide\">\n            {product.harvestYear}\n          </span>\n        </div>\n\n        {/* Rating Badge */}\n        <div className=\"absolute top-3 right-3 bg-white/90 backdrop-blur-md px-2 py-0.5 rounded-full shadow-sm flex items-center gap-1 text-xs font-semibold text-[#121A14]\">\n          <span className=\"text-[#C9A456]\">★</span>\n          <span>{product.rating}</span>\n          <span className=\"text-[#667069] text-[10px]\">({product.reviewsCount})</span>\n        </div>\n\n        {/* Terroir Stamp Pill on Hover */}\n        <div className=\"absolute bottom-3 left-3 right-3 bg-[#035224]/90 backdrop-blur-md text-white px-3 py-1.5 rounded-xl text-xs flex items-center justify-between opacity-0 group-hover:opacity-100 transition-opacity duration-200\">\n          <span className=\"truncate\">{product.origin.region}</span>\n          <span className=\"text-[#89AA3E] font-semibold shrink-0\">{product.origin.altitude}</span>\n        </div>\n      </div>\n\n      {/* Card Body */}\n      <div className=\"p-5 sm:p-6 flex flex-col flex-grow justify-between\">\n        <div>\n          {/* Category & Region */}\n          <div className=\"flex items-center justify-between text-xs text-[#89AA3E] font-semibold uppercase tracking-wider mb-1\">\n            <span>{product.categoryLabel}</span>\n            <span className=\"text-[#667069] normal-case tracking-normal\">{product.origin.altitude}</span>\n          </div>\n\n          {/* Title */}\n          <h3 className=\"font-display text-2xl sm:text-3xl text-[#035224] tracking-wide leading-none group-hover:text-[#89AA3E] transition-colors mb-1\">\n            {product.nameRu}\n          </h3>\n          <p className=\"text-xs text-[#667069] font-sans mb-3 italic\">\n            {product.nameOriginal}\n          </p>\n\n          {/* Taste Notes Pills */}\n          <div className=\"flex flex-wrap gap-1.5 mb-4\">\n            {product.tasteNotes.slice(0, 3).map((note, idx) => (\n              <span\n                key={idx}\n                className=\"px-2.5 py-1 rounded-full bg-[#EEE1D5]/70 text-[#121A14] text-xs font-medium border border-[#E2D7CC]/60\"\n              >\n                {note}\n              </span>\n            ))}\n          </div>\n\n          {/* Quick Brewing Preview */}\n          <div className=\"flex items-center gap-4 py-2 px-3 rounded-xl bg-[#FAF7F2] border border-[#E2D7CC]/60 text-xs text-[#667069] mb-4\">\n            <div className=\"flex items-center gap-1.5\">\n              <Thermometer className=\"w-3.5 h-3.5 text-[#D4583D]\" />\n              <span>{product.brewing.temp}°C</span>\n            </div>\n            <div className=\"flex items-center gap-1.5\">\n              <Clock className=\"w-3.5 h-3.5 text-[#035224]\" />\n              <span>{product.brewing.infusions[0]}–{product.brewing.infusions[product.brewing.infusions.length - 1]} сек</span>\n            </div>\n            <div className=\"text-[11px] text-[#89AA3E] font-medium ml-auto\">\n              {product.brewing.ratio}\n            </div>\n          </div>\n\n          {/* Weight Selectors (Chips) */}\n          <div className=\"mb-4\">\n            <div className=\"flex items-center justify-between text-[11px] text-[#667069] uppercase font-semibold tracking-wider mb-2\">\n              <span>Фасовка:</span>\n              <span className=\"text-[#89AA3E]\">{pricePerGram} ₽ / грамм</span>\n            </div>\n            <div className=\"grid grid-cols-2 sm:grid-cols-4 gap-1.5\">\n              {product.weights.map((w) => {\n                const isSelected = selectedWeight.weight === w.weight;\n                return (\n                  <button\n                    key={w.weight}\n                    onClick={() => setSelectedWeight(w)}\n                    className={`py-1.5 px-2 rounded-lg text-xs font-semibold tracking-tight transition-all text-center ${\n                      isSelected\n                        ? 'bg-[#035224] text-white shadow-sm ring-1 ring-[#035224]'\n                        : 'bg-[#EEE1D5]/50 text-[#121A14] hover:bg-[#EEE1D5] border border-[#E2D7CC]'\n                    }`}\n                  >\n                    {w.weight} г\n                  </button>\n                );\n              })}\n            </div>\n          </div>\n        </div>\n\n        {/* Footer: Price & Add to Cart Button */}\n        <div className=\"pt-4 border-t border-[#E2D7CC] flex items-center justify-between gap-3 mt-auto\">\n          <div>\n            <div className=\"text-[11px] text-[#667069] leading-none\">Итого:</div>\n            <div className=\"font-display text-2xl sm:text-3xl text-[#035224] leading-tight\">\n              {selectedWeight.price.toLocaleString('ru-RU')} ₽\n            </div>\n          </div>\n\n          <button\n            onClick={handleAdd}\n            className={`px-5 py-3 rounded-full text-xs font-bold uppercase tracking-wider transition-all duration-200 flex items-center gap-2 shadow-md active:scale-95 ${\n              isAdded\n                ? 'bg-[#89AA3E] text-white'\n                : 'bg-[#035224] hover:bg-[#023818] text-white'\n            }`}\n          >\n            {isAdded ? (\n              <>\n                <Check className=\"w-4 h-4\" />\n                <span>Добавлено</span>\n              </>\n            ) : (\n              <>\n                <ShoppingBag className=\"w-4 h-4\" />\n                <span>В корзину</span>\n              </>\n            )}\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n};\n